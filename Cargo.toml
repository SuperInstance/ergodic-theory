[package]
name = "ergodic-theory"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
repository = "https://github.com/SuperInstance/ergodic-theory"
description = "Ergodic theory in Rust — measure-preserving transformations, Birkhoff's theorem, entropy of dynamical systems, mixing"

[dependencies]
nalgebra = "0.33"
serde = { version = "1", features = ["derive"] }
approx = "0.5"

[dev-dependencies]
